# 求职PPT文字稿
## 基于U-Net语义分割的森林火灾过火区域检测

---

## 第1页 · 封面

**项目名称：** 基于U-Net深度学习的森林火灾过火区域遥感检测

**本人角色：** 本科毕业设计独立开发者 · 相关论文共同作者

**成果发表：** Remote Sensing（SCI，中科院二区），2023，15(7):1768

**关键词：** 深度学习 · 语义分割 · 遥感图像处理 · 类别不平衡 · U-Net

---

## 第2页 · 项目背景

**森林火灾的危害与监测需求**

- 全球气候变暖导致极端干旱频发，森林火灾规模持续扩大
- 传统人工巡查效率低、覆盖范围有限，无法满足大范围实时监测需求
- 卫星遥感具备大面积、周期性观测能力，是过火区域识别的重要手段

**核心挑战：严重的类别不平衡问题**

- 过火像素在整幅卫星图像中占比 **< 5%**，背景类别占比 **> 95%**
- 类别不平衡导致模型倾向于将所有像素预测为背景，漏检率极高
- 如何在极少正样本条件下提升火灾区域的检测召回率，是本项目的核心问题

---

## 第3页 · 本人具体工作

**一、数据集构建（独立完成）**

1. **数据来源与筛选**
   - 从美国地质调查局（USGS）下载 Landsat-8 OLI 卫星影像
   - 覆盖美国西海岸、澳大利亚东北部等重大火灾区域（2015—2021年）
   - 共处理 **40景卫星图像**，涵盖 **60余次**野火事件

2. **数据预处理**
   - 使用 FLAASH 算法对原始影像进行大气校正与辐射定标
   - 提取 RGB + 近红外（NIR）+ 短波红外（SWIR）共4个波段
   - 编写滑动窗口裁剪脚本（`crop_samples.py`），将大幅影像切割为 **512×512** 像素的训练样本块

3. **自动化标签生成**
   - 基于光谱指数设计自动标注算法（`tag_making.py`），参考国际主流阈值方法
   - 火灾像素判别准则（基于 SWIR/NIR 光谱比值与统计阈值）：
     - SWIR 反射率 > 0.8，NIR 反射率 > 0.4，海岸蓝波段 < 0.2
     - 同时结合 3σ 统计异常检测排除误标
   - 植被像素通过 NDVI 指数判定，最终生成三类语义标注（火灾 / 植被 / 背景），训练时合并为二分类（过火 / 非过火）

4. **数据集统计分析**
   - 编写脚本统计各类别像素占比，量化不平衡程度：
     - 火灾像素：**4.52%**（严重少数类）
     - 植被像素：**16.2%**
     - 背景像素：**79.28%**
   - 该分析结果直接支撑了已发表论文 **第2.2节** 的数据集描述

**二、模型训练与对比实验（独立完成）**

1. 搭建 U-Net 训练流程，基于 TensorFlow/Keras 框架
2. 设计并实施两组损失函数对比实验，探究类别不平衡解决方案：
   - 加权交叉熵损失（Weighted Cross Entropy，权重比 1:1.5）
   - Focal Loss（α=1，γ=3，聚焦难分样本）
3. 调整学习率、批大小等超参数，记录并分析训练曲线与验证曲线

---

## 第4页 · 技术方案

**整体流程**

```
Landsat-8 卫星影像
      ↓ 大气校正 + 辐射定标（FLAASH）
      ↓ 多波段融合（RGB + NIR + SWIR）
      ↓ 滑动窗口裁剪（512×512）
      ↓ 自动标签生成（光谱阈值 + 统计检测）
      ↓ U-Net 语义分割训练
      ↓ 分割结果评估（IoU、Recall、OA）
```

**U-Net 模型结构**

| 模块 | 说明 |
|------|------|
| 编码器（4层下采样） | 提取多尺度语义特征，通道数 64→128→256→512 |
| 瓶颈层 | 最深层特征提取，通道数 1024，含 Dropout(0.5) |
| 解码器（4层上采样） | 逐步恢复空间分辨率，通道数 512→256→128→64 |
| 跳跃连接 | 将编码器各层特征与解码器拼接，保留细粒度空间信息 |
| 输出层 | Softmax 激活，输出每像素的二分类概率图（512×512×2，过火 / 非过火） |

**训练配置**

| 参数 | 值 |
|------|----|
| 输入尺寸 | 512×512×3 |
| 批大小 | 2（受显存限制） |
| 最大轮数 | 50 epoch（含早停，patience=10） |
| 优化器 | Adam，初始学习率 1e-5 |
| 学习率策略 | ReduceLROnPlateau（val_loss 连续3轮不降则学习率×0.5） |
| 数据划分 | 训练60% / 验证40% |

---

## 第5页 · 解决类别不平衡的方法对比

**问题根源**

标准交叉熵损失对所有像素一视同仁，模型在极不平衡数据下会倾向于全部预测为背景，导致火灾漏检。

**方案一：加权交叉熵损失**

- 对少数类（火灾）赋予更高的损失权重
- 权重设置：为过火像素分配权重 1.0、非过火像素分配权重 1.5（`weight=[1.0, 1.5]`，类别顺序 burned:unburned）
- 原理简单，实现便捷，适合初步基线

**方案二：Focal Loss**

- 动态调整各样本的损失权重，对"容易预测"的样本降权，聚焦"难分"样本
- 参数设置：α=1，γ=3
- 相比加权交叉熵，更有针对性地提升少数类召回率

---

## 第6页 · 实验结果

| 配置方案 | 火灾区域 IoU | 总体精度（OA） | 火灾召回率（Recall） |
|----------|-------------|----------------|----------------------|
| 加权交叉熵（1:1.5） | 65.2% | 89.1% | 71.3% |
| **Focal Loss（α=1，γ=3）** | **68.7%** | 88.9% | **76.8%** |

**关键结论**

- Focal Loss 使火灾区域召回率从 71.3% 提升至 76.8%（提升 **5.5 个百分点**，相对提升约 **7.7%**），有效减少漏检
- 整体精度略有下降（约0.2%），是召回率提升的合理代价
- 在应急响应场景中，**漏检的代价远大于误报**，Focal Loss 更符合实际需求

---

## 第7页 · 项目亮点与个人技能体现

**项目亮点**

- 解决真实场景中的极端类别不平衡问题（1:20 比例），具有实际应用价值
- 完整实现了从原始卫星影像到语义分割结果的端到端流程
- 研究成果已发表于 **SCI 二区期刊 Remote Sensing**

**技术栈**

- **深度学习框架：** TensorFlow 2.x / Keras
- **遥感数据处理：** GDAL、FLAASH 大气校正
- **图像处理：** OpenCV、Pillow、NumPy
- **模型评估：** IoU、混淆矩阵、Recall、Overall Accuracy
- **编程语言：** Python

**可迁移能力**

- 图像语义分割模型的设计、训练与调优
- 非平衡数据集的处理策略（损失函数设计、数据增强）
- 多光谱/多波段数据的读取、预处理与分析
- 实验结果的系统记录与可视化分析

---

## 第8页 · 相关论文

**论文信息**

> **"A Dynamic Effective Class Balanced Approach for Remote Sensing Imagery Semantic Segmentation of Imbalanced Data"**
>
> *Remote Sensing*, 2023, 15(7): 1768
>
> DOI: [10.3390/rs15071768](https://www.mdpi.com/2072-4292/15/7/1768)

**本人贡献**

- 负责论文 **第2.2节** "Landsat-8 Forest Fire Burning Area Images" 的全部工作
- 包括：数据集构建方法设计、类别不平衡问题量化分析、实验数据集整理

---

*（文字稿仅供参考，可根据实际PPT版式调整各页详细程度）*
